/**
 * Array helpers
 */
import { ndarray, array } from './array/index';
export { ndarray, array };

export const add = (a: number, b: number): number => a + b;
export const subtract = (a: number, b: number): number => a - b;
